[package]
name = "async-wal-db"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
thiserror = "1.0"
crossbeam = "0.8"

[dev-dependencies]
tempfile = "3.0"  # For test WAL files